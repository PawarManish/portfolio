import React from 'react';
import Room from './components/room/Room';

export const App: React.FC = () => {
  return (
    <main className="app">
      <Room />
    </main>
  );
};

export default App;
